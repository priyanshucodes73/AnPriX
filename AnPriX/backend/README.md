
# AnPriX Backend (Express)
## Quick start
```bash
cd backend
npm install
npm run dev
```
- Runs on: http://localhost:4000
- Endpoints:
  - `/` (health)
  - `/api/tournaments`
  - `/api/teams`
  - `/api/matches/today`
